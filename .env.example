# Development-only SQL Server "sa" password used by docker-compose.yml.
# Must match the password in src/JournalApp.Api/appsettings.Development.json
# and meet SQL Server complexity rules (8+ chars, upper, lower, digit/symbol).
SA_PASSWORD=Journal_Dev_Passw0rd
